# ─── Python ───────────────────────────────
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info/
.python-version

# ─── 虚拟环境 ─────────────────────────────
.venv/

# ─── 密钥 / 环境（绝不入库）─────────────────
.env
.env.local
.env.bak*
*.key
*.pem

# ─── 工具缓存 ─────────────────────────────
.pytest_cache/
.ruff_cache/
.hermes/

# ─── 固化的运维流程（本地操作参考，不入远端；见用户指示 2026-07-06）────
# 发布/E2E 流程手册 + 脚本 + 每轮工作台账 = 本地操作工具，非交付产物，保留本地不 push。
scripts/release.sh
scripts/e2e_login.sh
docs/RELEASE_RUNBOOK.md
docs/E2E_RUNBOOK.md
docs/ROUND28_FINDINGS_TRIAGE.md

# ─── 运行时产物 / 数据 ────────────────────
.codegraph/
.qdrant-initialized
qdrant.log
swarm.log
*.log
*.log.*
logs/
snapshots/
workspace/
# greenfield 从零创建的项目内容（保留目录本身，忽略生成内容）
workdir/*
!workdir/.gitkeep

# ─── OS ───────────────────────────────────
.DS_Store

# ─── 内部开发日志（本地保留，不入库；GitHub README 用面向用户的版本）──
DEVLOG.md

# ─── 本地研发资料：界首 / 交接 / 就绪度 / 随跑日志（本地保留，不入库）──
E2E_*.md
*_HANDOFF.md
*_KICKOFF.md
*_TREATMENT_PLAN.md
*_READINESS.md
*_PROGRESS.md
UPSTREAM_DOWNSTREAM_TREATMENT.md
logs_archive/

# ─── 内部工作文档（不上云端；本地保留）──────────────
docs/archive/
/DESIGN_*.md
/PROJECT_STATUS.md
/TECH_DEBT.md
/WALKTHROUGH_REPORT.md

# ─── 内部设计文档树（不上云端；保留 docs/TEAM_TRIAL.md 对外）──
docs/*.md
docs/**/*.md
!docs/TEAM_TRIAL.md
cube-templates/NODE_ENVD_FIX.md

# 内部排查/审计/交接文档（本地工作件，绝不上传）
DEEP_READ_REGISTER_*.md
EXCEPT_AUDIT_*.md
PRODUCTION_READINESS_REGISTER.md
TASK_REGISTER_*.md
*_HANDOFF.md
E2E_ROUND*.md
UPSTREAM_DOWNSTREAM_TREATMENT.md
.env.lock

# 离线 record/replay 卡带（从 live checkpoint 抽取的 plan 快照；本地排障件，绝不上传）
cassettes/

# E2E 陪跑镜像/journal（会话working notes，本地排障用，不入库）
run_mirrors/

# 内部复盘/治本工作登记册（绝不入库——round65e6 用户纠正：这类文件不能提交）
*_POSTMORTEM_TREATMENT_REGISTER.md
*_TREATMENT_REGISTER.md
*_POSTMORTEM_*.md
